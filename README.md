# AFL Manager V14.1 — QA Patch

QA fixes:
- Corrected AFL Tables parser column mapping.
- Fixed malformed nested `<script>` markup and duplicate closing tag.
- Match engine now directly uses individual player attributes for production.
- Preserved V14 local-save compatibility.
- All inline JavaScript blocks pass Node.js syntax checking.

Ratings are custom game ratings, not official AFL/Champion Data ratings.
